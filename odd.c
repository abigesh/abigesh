#include <stdio.h>
#include <conio.h>
 
int main()
{
    int number;
    printf("enter the number: ");
    scanf("%d", &number);
    printf("The number is %s", (n%2==0 ? "EVEN" : "ODD"));
    return 0;
}
